import client from "./client";

/** API calls for /notification/api/v1 endpoints. */
const notificationsApi = {
  /** Base path for all notifications endpoints. */
  base: "/notification/api/v1",
};

export default notificationsApi;
