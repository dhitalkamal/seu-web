import client from "./client";

/** API calls for /payment/api/v1 endpoints. */
const paymentApi = {
  /** Base path for all payment endpoints. */
  base: "/payment/api/v1",
};

export default paymentApi;
