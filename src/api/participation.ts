import client from "./client";

/** API calls for /participation/api/v1 endpoints. */
const participationApi = {
  /** Base path for all participation endpoints. */
  base: "/participation/api/v1",
};

export default participationApi;
