/** CheckoutPage -- implement when building this feature. */
export default function CheckoutPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-gray-500">CheckoutPage</p>
    </div>
  );
}
