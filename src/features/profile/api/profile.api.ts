// import client from "@/shared/api/client"; -- uncomment when implementing

/** API calls for the profile feature. */
const profileApi = {
  base: "/iam/api/v1/profile",
};

export default profileApi;
