/** TicketsPage -- implement when building this feature. */
export default function TicketsPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-gray-500">TicketsPage</p>
    </div>
  );
}
