/** EventsPage page -- implement when building this feature. */
export default function EventsPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-gray-500">EventsPage</p>
    </div>
  );
}
