/** LoginPage page -- implement when building this feature. */
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-gray-500">LoginPage</p>
    </div>
  );
}
